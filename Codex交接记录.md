# 车乐租二手个人车商入网原型交接记录

本文档用于在另一个 Codex 账号继续开发/调整当前静态原型。项目目录：

`C:\Users\Tang\Documents\车乐租二手个人车商入网`

首页地址：

`file:///C:/Users/Tang/Documents/%E8%BD%A6%E4%B9%90%E7%A7%9F%E4%BA%8C%E6%89%8B%E4%B8%AA%E4%BA%BA%E8%BD%A6%E5%95%86%E5%85%A5%E7%BD%91/home.html`

## 页面文件

- `home.html`：首页。点击金刚区 `车商管理` 进入 `list.html`，点击底部 `订单` 进入 `order.html`。
- `list.html`：车商列表页，在原一级/二级经销商数据基础上增加二手个人车商数据。
- `index.html`：车商新增页，目前主要用于二手个人车商新增，同时兼容一级/二级经销商字段展示。
- `detail.html`：车商查看页。
- `order.html`：订单列表页。点击订单卡片进入 `order-detail.html`，底部首页可返回 `home.html`。
- `order-detail.html`：订单详情页，包含材料清单入口、AI 全局检测、演示用重置按钮。
- `material-detail.html`：材料清单详情页，支持附件上传、删除、AI 附件检测、AI 全局检测结果展示。
- `assets/home-hero-truck.svg`：首页顶部静态底图。
- `assets/home-tesla-model3.jpg`：之前下载的 Tesla 图片，目前首页已改用 `home-hero-truck.svg`。

## 主要业务背景

原系统已有商用车车商入网功能，之前主要是公户入网。新增需求是支持二手个人车商入网：

- 车商级别增加 `个体`，车商级别选项为：`一级`、`二级`、`个体`。
- 选择 `个体` 时隐藏顶部导航模块、门店基本信息、工商信息、联系人信息。
- 选择 `一级` 或 `二级` 时，车商级别下方展示步骤导航，并展示对应字段。
- 二手个人车商新增字段包括姓名、联系方式、是否中介、业务产品、业务类型、展业城市、车商评级、主流销售品牌、收款账号、证明材料、特殊说明等。

## 新增页 index.html 当前规则

### 字段顺序与展示

- `车商级别` 在最上面。
- `个体` 模式隐藏门店基本信息、工商信息、联系人信息、步骤导航。
- `一级/二级` 模式显示步骤导航：`工作信息`、`门店基本信息`、`联系人信息`、`收款账号`。
- 步骤导航点击后会自动滚动定位到对应模块，并在滚动时自动高亮当前模块。
- 业务类型显示为 `二手车`。
- `展业城市` 字段已移动到 `车商评级` 上方。
- `展业城市`、`主流销售品牌` 字段要求名称左对齐、已选内容靠右展示，选项过多时自动换行。

### 展业城市弹窗

- 原本有 `我的展业城市` 和 `全国城市` 两个页签，已合并为一个分层选择。
- 左侧省级列表第一项为 `我的展业城市`，默认选中。
- 其他省份为全国城市列表。
- 选择不在本人展业范围内的城市时，需要提示，并要求上传 `跨区域入网特批材料`。
- 已选城市在弹窗内有已选区域，支持点击取消。
- 选中城市在字段中反显。

### 主流销售品牌

- 点击跳转到品牌选择页。
- 有已选择品牌区域，支持直接取消选中。
- 选择完成后回显到新增页字段。

### 收款账号

- 模块沿用原有样式。
- 字段 `车商登记类型` 已改名为 `账户类型`。
- 选择 `个体` 时 `账户类型` 不可编辑。
- 选择 `一级/二级` 时 `账户类型` 可编辑。
- 收款账号其他字段支持编辑。

### 证明材料

证明材料名称强制左对齐。一类材料一行，每类材料支持多个附件：

- 初始每个材料只显示一个上传入口。
- 每上传一个附件，该入口变为 `已上传`，并在后面自动补一个新的上传按钮。
- 每行最多展示 3 个附件，超过自动换行。
- 删除某个附件后会保留或补回一个上传入口。
- `跨区域入网特批材料` 只有在展业城市选择了跨区域城市时展示。
- 当前材料包括：身份证、打卡照、收款证明、银行卡正面、佐证从业经验资料、跨区域入网特批材料、合作商合作协议、二手个人征信报告、其他材料。

## 车商查看页 detail.html

- 顶部名称从 `张三二手个人车商` 改为 `张三`。
- 顶部删除了 `已入网`、`核心车商` 标签。
- `审批日志` 按钮放在张三旁边。
- 点击审批日志后底部弹窗展示日志。
- 下方页签合并为一个 `基础信息`，删除审批日志页签。
- 证明材料样式参考新增页：一类材料一行，每类材料可有多个。
- 查看页材料不显示 `非必填`，不标红，普通样式。

## 车商列表页 list.html

- 不是全部数据都是二手个人，而是在原一级/二级经销商基础上加入二手个人数据。
- 二手个人卡片姓名左对齐展示。
- 二手个人下方隐藏联系方式、姓名，新增 `负责人` 字段。
- 删除二手个人的城市字段，保留 `展业城市`。
- `展业城市` 和 `负责人` 放一行，放在渠道商下面一行。
- 渠道商和负责人信息已向左调整。
- 展业城市只占一行。
- 删除二手个人标签中的 `混合新车上`、`中介`、`跨区域`、`核心车商`。
- 卡片图宽度统一。
- 列表页右侧改为筛选图标。
- 顶部状态栏做得更丰富，电池有电量。
- 搜索框清空按钮已换成更精致的 SVG 叉图标。

## 首页 home.html

- 首页可以不复杂，重点是作为入口。
- 金刚区图标已换成 Remix Icon 风格的实心 SVG，黑红为主。
- 底部导航图标已调整为和订单页一致的线性风格。
- 顶部背景已改为静态图片覆盖，使用 `assets/home-hero-truck.svg`，不再叠加原来的文字和车图层。
- 点击金刚区 `车商管理` 进入 `list.html`。
- 点击底部 `订单` 进入 `order.html`。

## 订单列表页 order.html

- 页面按用户提供的订单列表例图进行复刻。
- 删除横向滚动条。
- 点击 `李晓丽` 订单卡片进入 `order-detail.html`。
- 点击第一条 `王建国` 订单卡片也进入 `order-detail.html`。
- 第一条订单标题从 `海南盛世吉元汽车贸易有限公司` 改为 `王建国`。
- 底部导航的首页点击回到 `home.html`。
- 底部导航图标和首页保持一致。
- 搜索按钮缩小并放进搜索框。
- 搜索图标已替换为更好看的样式。

## 订单详情页 order-detail.html

### 基础跳转

- 左上返回按钮点击返回订单列表页 `order.html`。
- 材料清单模块点击进入 `material-detail.html`。
- 材料清单入口的右箭头贴近页面右侧。
- `已录入` / `未录入` 状态左对齐。

### 材料清单录入状态

- 订单详情页材料清单的录入状态根据材料清单详情页的必填材料是否上传自动判断：
  - 必填材料全部上传：`已录入`
  - 否则：`未录入`

### AI 全局检测按钮

- `AI全局检测` 按钮放在材料清单模块下面，仅按钮展示，不包成模块。
- 按钮缩小并靠右。
- 点击按钮：
  - 如果材料清单中存在附件检测中的附件，toast 提示：`XXXAI附件检测中，请检测完成后重新操作`
  - 如果存在附件检测不通过材料，弹窗提示：`XXXAI附件检测不通过，可能导致AI全局检测不准确，是否继续`
  - 确认后开始 AI 全局检测。
- 检测中按钮变为：`AI全局检测中（预计30s）`。
- 检测大约 20 秒后自动出结果，当前模拟结果为不通过。
- 检测中时，材料详情页只允许查看，不允许新增、删除附件，也不展示编辑按钮。
- 检测通过或不通过后，材料清单详情页可继续编辑。

### AI 全局检测提示

订单详情页材料清单下方提示规则：

- 未检测：`点击AI全局检测，提前发现错误，预计30s出结果`
- 检测中：`AI全局检测中，检测期间无法上传材料。`
- 检测不通过：`AI全局检测完成，存在待关注项`
- 检测通过：`AI全局检测完成，未发现问题`
- 检测完成后如果材料详情页又上传了新附件：`你有新上传的附件，建议你重新AI全局检测`

颜色规则：

- 未检测、新上传建议重检：灰色。
- 检测中：蓝色。
- 检测不通过：橙色。
- 检测通过：绿色。

提示已和 `已录入/未录入` 起点对齐，宽度不够时允许换行。

### 演示用重置按钮

- 订单详情页有悬浮按钮：`AI全局检测状态重置`，放在页面左下侧。
- 点击后：
  - AI 全局检测状态变为未检测。
  - 清空全局检测结果。
  - 清空材料清单详情页所有附件的附件检测状态和检测意见。
  - 清空全局检测意见。
  - 再进入材料详情页时显示普通未检测样式。

## 材料清单详情页 material-detail.html

### 页面结构

- 从订单详情页材料清单入口进入。
- 顶部删除了 `客户人脸验证未通过`。
- `汽车融资租赁确认函` 改为 `首付款凭证`。
- `发票或发票后置承诺函` 改为 `登记证`。
- 新增 `担保个人-唐慧峰`，材料包括：保证担保合同、合同面签照。
- 材料支持折叠，折叠后只展示材料名称和数量。
- 当前材料如果有 AI 审核不通过，要展示对应标识。
- `承租人`、`担保人` 旁有全局折叠/展开按钮。
- `银行流水/微信流水/支付宝流水` 已改为 `发票`。
- 材料和材料之间用颜色块区分，车架号之间不需要颜色块区分。

### 附件上传

- 每个材料支持上传多个附件。
- 每个附件单独占一行。
- 上传按钮和附件样式有区分，上传按钮更突出，新增材料类型按钮弱化并居中。
- 附件上传后显示更丰富信息，例如附件类型、上传时间等。
- 附件支持删除，删除按钮是右上角减号样式。
- 非必填材料也支持删除。

### AI 附件检测

- 部分材料支持 AI 识别，会有 AI 标记，标记靠页面右侧。
- `保证担保合同` 和 `合同面签照` 不需要 AI 标记。
- 上传附件后展示 `附件检测中`，有动态效果，持续约 8 秒。
- AI 检测通过后不需要特殊样式，正常展示。
- AI 检测不通过时，审核意见展示在附件内部，不单独拆成两块。
- 审核意见可能有多个、多行，字体颜色为橙色。
- 附件检测意见主要围绕：清晰度、是否有水印、是否反光、是否翻拍、上传附件类型是否正确。

### AI 全局检测意见

- AI 全局检测主要做附件内容之间的核对，例如车架号是否正确、客户姓名是否正确。
- AI 全局检测不通过后，附件不整体高亮，只展示审核意见颜色变化。
- 附件的不通过意见分两类：
  - `附件检测意见`
  - `全局检测意见`
- 如果附件本身没有附件检测失败原因，但全局检测不通过，只展示全局检测意见。
- 如果附件在 AI 全局检测前已有附件检测意见，则全局检测后同时展示附件检测意见和全局检测意见。

### 车辆维度材料

以下材料按车辆维度上传，展示车架号：

- 首付款凭证
- 登记证
- 合格证或订单截图

`登记证` 在同一个材料下分子页：

- 登记证1、2页
- 登记证3、4页
- 登记证5、6页
- 登记证7、8页
- 登记证尾页

登记证子页规则：

- 每个子页只能上传一张。
- 未上传时每个子页有单独上传入口。
- 登记证7、8页和登记证尾页的未上传样式有示例图，和正常附件缩略图区分。
- 未上传示例图上有半透明白色相机图标。
- 上传后把示例图替换为上传后的图片缩略图。

### 合格证或订单截图规则

- 默认材料名为 `合格证或订单截图`，默认为空。
- 点击任意车架号下面上传附件时弹出底部弹窗，选项：`合格证`、`订单截图`。
- 选择后整个材料名称改为所选材料名称，不是每个车架号单独改。
- 后续上传附件都使用这个材料类型。
- 材料名称旁边有 `切换` 按钮。
- 点击切换可再次弹出底部弹窗。
- 如果切换材料类型且已经上传附件，提示：`当前材料已经上传附件，切换为新的材料类型将会清空原附件`
- 点击确定后切换材料类型并清空原附件。

## 状态持久化

因为都是本地静态 HTML，页面之间通过 `localStorage` 和 `window.name` 做状态保存。

重点状态：

- `materialChecklistState`：材料清单详情页的材料和附件状态。
- `materialGlobalStatus`：AI 全局检测状态，可能为空、`checking`、`pass`、`fail`。
- `materialGlobalStartedAt`：AI 全局检测开始时间。
- `materialGlobalRunId`：AI 全局检测运行 id，用于避免重置后旧定时器又改回状态。
- `materialGlobalResult`：AI 全局检测结果与意见。
- `materialGlobalDirty`：全局检测完成后是否又上传了新附件。
- `materialDetectionReset`：演示用重置标记。

需要注意：

- 用户明确要求从材料清单详情页返回订单详情页时，要记住 AI 全局检测状态。
- 再次从订单详情页进入材料清单详情页时，要保留之前上传的附件和 AI 审核结果。
- 不能因为返回或重新进入页面就刷新丢失状态。

## 视觉偏好和注意事项

- 用户非常关注对齐、左右位置、是否换行、按钮是否难看。
- 不要用粗黑边框图标。
- 图标尽量用成熟图标库风格，如 Remix Icon / Lucide 风格。
- 不要只“加底色”冒充换图标。
- 横向滚动条必须避免。
- 文案和状态位置要严格跟用户标注处对齐。
- 用户会在浏览器里点选元素后要求细调，优先小范围改动。

## 常用检查命令

检查某个 HTML 内脚本语法：

```powershell
$node='C:\Users\Tang\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe'
& $node -e "const fs=require('fs'); const html=fs.readFileSync('index.html','utf8'); const m=html.match(/<script>([\s\S]*?)<\/script>/); new Function(m[1]); console.log('index script syntax ok');"
```

根据文件替换 `index.html` 即可检查其他页面。

## 最近一次上下文

最近用户在 `order-detail.html` 调整 AI 全局检测提示：

- 未检测提示：`点击AI全局检测，提前发现错误，预计30s出结果`
- 新上传附件提示：`你有新上传的附件，建议你重新AI全局检测`
- 这两个提示颜色改为灰色。
- 提示整体往前移，并允许宽度不够时换行。

随后用户在 `index.html` 要求：

- `展业城市` 字段放到 `车商评级` 上面。

该调整已经完成。
